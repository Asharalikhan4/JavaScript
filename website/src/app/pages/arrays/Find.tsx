import SubHeading from "@/app/components/SubHeading";
import React, { FC } from "react";

const Find: FC = () => {
    return (
        <div id="find">
            <SubHeading name="Find" />
        </div>
    );
};

export default Find;